#include<stdio.h>
int main(){
	int i,n,sum=0,c=-1;
	printf("enter the end value n :");
	scanf("%d",&n);
	for (i=1;i<=n;i++){
		c=c+2;
		if (c<=n){
			sum=sum+c;
		}
	}
	printf("sum of the odd numbers until %d is %d",n,sum);
	return 0;
}
